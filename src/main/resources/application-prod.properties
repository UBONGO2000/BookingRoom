# Activated with SPRING_PROFILES_ACTIVE=prod.
# Requires the database schema to already exist (ddl-auto=validate does not create/alter tables).
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
